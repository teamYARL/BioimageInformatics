Run the autoCalibraton.m to get the pixel size calibration.

In the autoCalibration.m, update the ratio,distance,filename for different image inputs.

When execute the autoCalibration.m, first crop a region long enough in X axis which 
should include some black segments, then crop a relatively small background region as 
information needed for local minimums detection. 
